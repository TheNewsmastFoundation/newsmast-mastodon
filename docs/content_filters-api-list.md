# content_filters API Inventory

Generated on: 2026-05-26

## Scope and Method

This inventory was generated by scanning route and controller sources inside this gem only:

- lib/content_filters/engine.rb
- test/dummy/config/routes.rb
- config/initializers/prepend_concerns.rb
- app/lib/content_filters/overrides/home_extended_timeline.rb
- app/lib/content_filters/overrides/public_extended_timeline.rb

## API Endpoints Found

No API routes are declared in this gem.

- Engine mount exists in host app routing via initializer, but the gem does not define an engine routes file with API endpoints.
- Dummy app defines only a health route at /up (non-API).

### Endpoint Table

| HTTP Method | Full Path | Controller#Action | Auth | Path Params | Query Params | Body Params |
|---|---|---|---|---|---|---|
| None | None | None | None | None | None | None |

## Non-API Route Detected (Excluded from API Inventory)

| HTTP Method | Full Path | Controller#Action | Auth |
|---|---|---|---|
| GET | /up | rails/health#show | Public |

## Route-Adjacent Controller Overrides (Behavior Changes, Not New Routes)

These modules modify host Mastodon timeline behavior but do not declare routes in this gem:

- Api::V1::Timelines::HomeController prepends method home_statuses
  - Common query params observed: max_id, since_id, min_id, exclude_direct_statuses, exclude_followed_tags, exclude_replies, grouped_admin_statuses
- Api::V1::Timelines::PublicController prepends method public_feed
  - Common query params observed: local, remote, limit, only_media, with_reblogs, grouped_admin_statuses

## Postman Notes

Because no API routes are declared by this gem, the generated Postman collection intentionally contains no request items. It includes only collection-level variables for host integration:

- base_url
- access_token
